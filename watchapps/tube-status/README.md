# tube-status

Pebble watchapp that displays the current status of the transit network lines.

The project manages two applications - Tube Status and Transit Status.

## Building

Build an app using the build script and including the name of the app:

```
./build.sh $NAME
```

> Make sure the application is configured to behave as the correct app before publishing!
