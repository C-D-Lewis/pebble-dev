function handleSyncUpdate(dict) {
  // TODO: How to serialize the C structs for AppMessage transport in an easily
  //       extendable way?

  // timestamp field is the unique ordering key
}

module.exports = {
  handleSyncUpdate,
};
